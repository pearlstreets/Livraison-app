node_modules
.expo
.expo-shared
npm-debug.log
# package-lock.json volontairement NON ignoré : il est suivi par git (la règle
# était inerte pour git) mais EAS construit son archive depuis .gitignore, donc
# l'exclure faisait perdre la détection npm -> EAS retombait sur `yarn install`
# (absent de la machine) et le build local échouait. Cf. build Android 1003.
dist/
ios/
android/
.claude/
backup_*/
historique/
retry.txt

historique/reverts/*.bak

# Cle de service Google Play (secret)
google-play-service-account.json

# Artefacts de build et sauvegardes locales (ne pas versionner)
build/
ios.bak-*/
assets/_icon_backup_*/
*.png.bak
_SAUVEGARDES_archive_*.tar.gz
ANDROID_BUILD_STATUS.txt
DELIVERY_BUILD_STATUS.txt
